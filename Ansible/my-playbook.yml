---
  - name: My first playbook
    hosts: webservers
    become: true
    tasks:

    - name: Install apache http (state=present is optional)
      apt:
       name: apache2
       state: absent




